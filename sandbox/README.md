# sandbox

Local experimentation area. Everything in this directory is **gitignored** — only this
README is kept in git. Scratch files, experiments, and the files `make run-chrome`
writes (`courtyard.log`, `courtyard.pid`) live here and never reach the repository.
