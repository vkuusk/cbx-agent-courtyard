"""Dummy: the fake agent used to build and feel the hub before real agents exist.

It is the reference implementation of the adapter contract (design §7.4) — it uses the
same client library and endpoints a real adapter does; nothing about the hub is mocked.
"""
